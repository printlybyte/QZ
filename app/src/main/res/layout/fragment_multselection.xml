<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/comm_bg"
    android:orientation="vertical">

    <RelativeLayout style="@style/titlebar">

        <ImageView
            android:id="@+id/leftimg"
            style="@style/topleftimg" />

        <TextView
            android:id="@+id/title"
            style="@style/basetitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true" />
    </RelativeLayout>

    <ListView
        android:background="@color/white"
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:divider="@color/transparent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/firstitem_margintop"
        android:cacheColorHint="@color/transparent"
        android:listSelector="@color/transparent"></ListView>

    <TextView
        android:text="自定义日期"
        android:layout_marginTop="@dimen/firstitem_margintop"
        android:layout_width="match_parent"
        android:layout_height="40dp" android:drawableRight="@drawable/showchid" android:paddingLeft="20dp" android:paddingRight="15dp" android:background="@drawable/dep_emp_selector"/>
</LinearLayout>